# yaml-language-server: $schema=https://github.com/dfinity/icp-cli/raw/refs/tags/v0.1.0-beta.2/docs/schemas/icp-yaml-schema.json

canisters:
  - name: sender
    recipe:
      type: "@dfinity/motoko"
      configuration:
        main: "src/sender.mo"
        args: ""

  - name: receiver
    recipe:
      type: "@dfinity/motoko"
      configuration:
        main: "src/receiver.mo"
        args: ""
